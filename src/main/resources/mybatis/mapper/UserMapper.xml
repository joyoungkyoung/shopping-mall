<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nicky.shoppingmall.domain.user.mapper.UserMapper">

    <update id="modifyAddressId" parameterType="ModifyAddressIdDto">
        /* UserMapper::modifyAddressId */
        update user set address_id=#{addressId} where id=#{id}
    </update>
</mapper>